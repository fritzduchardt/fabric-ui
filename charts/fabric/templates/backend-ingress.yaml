apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fabric-ui
spec:
  ingressClassName: nginx
  rules:
    - host: fabric-backend.friclu
      http:
        paths:
          - backend:
              service:
                name: fabric-backend
                port:
                  number: {{ .Values.backend.port }}
            path: /
            pathType: ImplementationSpecific
{{/*  tls:*/}}
{{/*    - hosts:*/}}
{{/*        - ha-friclu.duckdns.org*/}}
{{/*      secretName: ha-certificate*/}}
